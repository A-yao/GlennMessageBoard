@model IEnumerable<GlennMessageBoard.Models.Forum>

@{
    ViewBag.Title = "Index";
}

<h2>留言版</h2>

<p>
    @Html.ActionLink("新增版面", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TITLE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BUD_DTM)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.TITLE, "Details", new { id = item.ID }, null)
            @*@Html.ActionLink(item.TITLE, "index", "ARTICLE", new { FORUM_ID = item.ID }, null)*@
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BUD_DTM)
        </td>
        <td>
            @Html.ActionLink("編輯", "Edit", new { id=item.ID }) |
            @Html.ActionLink("刪除", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
